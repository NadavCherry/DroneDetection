"""Model architecture YAMLs (read by ultralytics) and experiment configs (read by us).

This file exists only so that ``configs.experiments`` is an importable package. The
``*.yaml`` siblings are ultralytics model definitions and are passed to it by path;
nothing in Python parses them.
"""
