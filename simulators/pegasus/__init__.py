"""Pegasus (Isaac Sim + PX4) simulator bindings for air-to-air drone detection.

The simulator platform itself -- the Iris airframe, the camera calibration, the
Isaac Sim 6.0.1 compatibility work -- lives in a separate, external robotics
platform (see the Prerequisites section of this package's README). This package
is the SpeckLock side: the
outdoor scenes, the two-drone geometry, and the recordings that come out.
"""
